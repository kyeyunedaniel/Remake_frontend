import { createTheme } from "@mui/system"

// import {createTheme}
export const theme = createTheme({
    palette:{
        primary:{
            main:'skyblue',
            light: 'navyblue'
        },
        secondary:{
            main:'black'
        }
    }
})